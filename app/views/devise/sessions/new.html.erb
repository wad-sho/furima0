<%= render "shared/second-header"%>

<%= form_with model: @wada,url: new_wada_session_path,class: 'registration-main', local: true do |f| %>
<%# これないとログイン失敗 %>
<div class='form-wrap'>
  <div class='form-header'>
  <%= link_to 'Facebookでログイン', wada_facebook_omniauth_authorize_path, method: :post %>
<%= link_to 'Googleでログイン', wada_google_oauth2_omniauth_authorize_path, method: :post %>
    <h1 class='form-header-text'>
      会員情報入力
    </h1>
  </div>
  <div class='login-flash-message'>
    <%= flash[:notice] %>
    <%= flash[:alert] %>
  </div>
  <div class="form-group">
    <div class='form-text-wrap'>
      <label class="form-text">メールアドレス</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true %>
  </div>
  <div class="form-group">
    <div class='form-text-wrap'>
      <label class="form-text">パスワード</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.password_field :password, class:"input-default", id:"password", placeholder:"" %>
  </div>
  <div class='login-btn'>
    <%= f.submit "ログイン" ,class:"login-red-btn" %>
  </div>
</div>
<% end %>

<%= render "shared/second-footer"%>